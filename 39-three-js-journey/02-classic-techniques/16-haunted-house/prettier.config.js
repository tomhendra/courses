/** @type {import("prettier").Config} */
const config = {
  trailingComma: 'es5',
  semi: false,
  useTabs: false,
  tabWidth: 2,
  singleQuote: true,
  arrowParens: 'always',
  printWidth: 80,
}

export default config
